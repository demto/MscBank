@model MScBank.ViewModels.NewAccountViewModel
@{
    ViewBag.Title = "DebitAccountForm";
}

<h2>Open new Savings Account</h2>
<br />
<p>A credit card is for day to day use which can serve as contingency funds and can improve credit rating if used smart. Current interest is 20%.</p>
<p>Currently we provide with an online based credit card which has no physical card.</p>
<br />
<p>A loan is a one off big payment into your account which can be used for several different things incuding a bigger purchase such as a car</p>
<br />
<p>(but not a home) or paying off existing debt. It cannot be used for gambling, for unlowful purposes, for investments or for buying a property. Current rate is 10%</p>
<br />
<p>A mortgage is a big loan secured against a home which is primarily used for a home purchase. Current rate is 2%</p>
<br />
<p>Please enter an opening balance: </p>
<br />

@using (Html.BeginForm("CreateCreditCardAccount", "NewAccount")) {

    <div class="form-group">
        @Html.LabelFor(m => m.CreditCard.Limit)
        @Html.TextBoxFor(m => m.CreditCard.Limit, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.CreditCard.Limit)

        <br /><br />

        @Html.LabelFor(m => m.CreditCard.Name)
        @Html.TextBoxFor(m => m.CreditCard.Name, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.CreditCard.Name)
    </div>
    <button class="btn btn-primary" type="submit">Open</button>
}

